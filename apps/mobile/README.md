# Mobile (Fase 2)

Placeholder do módulo mobile. Na Fase 2, um app **Expo** ou **React Native** deve:

- Importar `@repo/shared` (`createClientSchema`, `createClientApi`, `validateCpf`)
- Usar `EXPO_PUBLIC_API_URL` apontando para a mesma API (`/api/v1/clients`)
- Reutilizar o mesmo fluxo de cadastro e feedback síncrono da resposta HTTP

Nenhum código nativo é necessário na Fase 1.
